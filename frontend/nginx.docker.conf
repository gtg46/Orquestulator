# Nginx configuration for Docker containerized deployment
# This config is optimized for use within Docker Compose environment
# - Uses Docker service names for backend communication
# - Uses container filesystem paths
# - Standard nginx logging locations

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    server {
        listen 80;  # Standard HTTP port for container
        server_name _;

        # Container path where built frontend files are copied
        root /usr/share/nginx/html;
        index index.html;

        # Proxy API requests to backend service using Docker Compose service name
        location /api/ {
            proxy_pass http://backend:7618/api/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Serve frontend SPA with fallback to index.html for client-side routing
        location / {
            try_files $uri $uri/ /index.html;
            add_header X-Frame-Options "SAMEORIGIN" always;
            add_header X-Content-Type-Options "nosniff" always;
            add_header X-XSS-Protection "1; mode=block" always;
            add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        }

        # Optimize static asset caching
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            access_log off;
        }

        # Enable gzip compression
        gzip on;
        gzip_vary on;
        gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

        # Use standard nginx log locations within container
        access_log /var/log/nginx/access.log;
        error_log /var/log/nginx/error.log;
    }
}