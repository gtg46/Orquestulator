services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "7618:7618"
    environment:
      - PYTHONPATH=/app
    networks:
      - orquestulator-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - orquestulator-network

networks:
  orquestulator-network:
    driver: bridge
