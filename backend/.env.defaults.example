# Orquestulator Default Environment Configuration

# BACKEND_HOST & BACKEND_PORT are used in the start_server.sh script to set the host and port for the backend server.
# Listen on localhost only (reverse proxy recommended for production)
BACKEND_HOST=127.0.0.1
BACKEND_PORT=7618

# Authentication
PASSPHRASE_AUTH=true
PASSPHRASE=Ch@ngeMe

## Session Management
SESSION_COOKIE_NAME=session_id
SESSION_TIMEOUT_HOURS=4
SESSION_COOKIE_SECURE=false
SESSION_COOKIE_SAMESITE=lax
PROACTIVE_SESSION_CLEANUP=true

# Rate Limiting
AUTH_RATE_LIMIT=5/minute

# API Documentation - Swagger UI provides interactive API documentation
SWAGGER_UI=true

# StackStorm Configuration
STACKSTORM_CONNECTIONS_CONFIG=./config/stackstorm-connections.json
